# 项目进度

> 最后更新：2026-09-04

## 总体状态

- 阶段：主窗单一列表壳（侧栏归类）；助理 + 暗色主题 + zh-CN/en-US
- 代码：Tauri 2 + SQLite + 主窗工作台 + 助理 + 暗色主题 + zh-CN/en-US
- 文档：[产品需求.md](产品需求.md) + [技术设计.md](技术设计.md) + [架构蓝图.md](架构蓝图.md)（跨项目复用）

---

## v1.0 Core

| 模块 | 状态 | 备注 |
|------|------|------|
| Tauri 2 项目脚手架 | ✅ 已完成 | Vue 3 + TS + 分层模块 |
| Rust Backend 分层架构 | ✅ 已完成 | domain / repository / services / commands |
| SQLite Schema + Migration | ✅ 已完成 | todos / reminders / settings / events / tags；with_tx |
| TODO CRUD Commands | ✅ 已完成 | 8 个 Todo Command（含 list_all_tags） |
| Todo/Done 两态 | ✅ 已完成 | 已由 v1.1 四态替代 |
| Reminder none + Scheduler | ✅ 已完成 | 30s tick + 启动补发 |
| 系统通知 | ✅ 已完成 | plugin + PendingNavigation + AUMID |
| 主窗口 UI + 详情面板 | ✅ 已完成 | 已由 v1.3 工作台取代 |
| 系统托盘 + 后台常驻 | ✅ 已完成 | 关窗隐藏 + 托盘菜单 |

## v1.1 Float

| 模块 | 状态 | 备注 |
|------|------|------|
| 助理 UI | ✅ 已完成 | Chrome/Body 双窗 + 圆球/条/面板；球条材质与微交互抛光 |
| 四态状态机 + due_date | ✅ 已完成 | migration 003 + 完整流转规则 |
| 上次停放记忆 | ✅ 已完成 | companion.placement + home_shape + ball_pos |
| 托盘左键切换助理 | ✅ 已完成 | toggle_floating_window |
| 默认形态当家 | ✅ 已完成 | ball 临时面板；panel 无圆球 |
| 边缘吸附 | ✅ 已完成 | 拖条换边/undock；拖面板只改 Y |
| 可选悬停预览 | ✅ 已完成 | 仅实色条命中；预览单击钉住不抢焦点 |

## v1.2 Polish

| 模块 | 状态 | 备注 |
|------|------|------|
| 标签 + 筛选 | ✅ 已完成 | migration 004 + list_all_tags |
| Snooze + daily/weekly | ✅ 已完成 | snooze_reminder + advance 周期计算 |
| events 审计 | ✅ 已完成 | migration 005，后端写入；主窗可选活动条 |
| 开机自启 + 托盘 badge | ✅ 已完成 | tauri-plugin-autostart + tooltip 计数 |

## v1.3 Workbench

| 模块 | 状态 | 备注 |
|------|------|------|
| 单一列表壳 | ✅ 已完成 | 侧栏全部/标签/归档；默认 all；无今天|库双模式 |
| 回收站 / 归档视图 | ✅ 已完成 | restore_todo + includeDeleted |
| Inspector autosave | ✅ 已完成 | 状态动词、标签 chips、提醒改期 |
| 时间语义（列表/Inspector） | ✅ 已完成 | `timeMeta`；独立 `TimePlannerPane` / `list_time_planner` 已移除 |
| 快捷键 / 空态 / 视觉 | ✅ 已完成 | `/ n j k Esc x`；冷静工具风 |
| 暗色主题 + i18n | ✅ 已完成 | ui.theme / ui.locale；vue-i18n zh-CN/en-US |

状态标记：`⬜ 未开始` / `🔄 进行中` / `✅ 已完成` / `⏸ 暂停`

交付清单见 [产品需求.md §8](产品需求.md#8-交付清单)

---

## 变更日志

- 2026-08-10：项目初始化，创建需求文档
- 2026-08-10：需求合并为产品需求 + 技术设计；分期 v1.0/v1.1/v1.2
- 2026-08-10：初始化 Tauri 2 + Vue 3 脚手架，Rust 分层目录与 health_check
- 2026-08-10：mise 环境对齐（Node 24 / Rust stable + tasks）
- 2026-08-10：v1.0 Core 实现 — SQLite、CRUD、Scheduler、托盘、主窗口 UI
- 2026-08-10：v1.0 自检与优化 — 类型/组件拆分、CreateTodoModal、注释补全
- 2026-08-10：v1.0 问题修复 — 通知恢复 plugin 唯一路径；详情抽屉；筛选栏可收起/固定
- 2026-08-10：v1.0 自检与文档同步 — 注释中文化；`@/` 路径别名
- 2026-08-10：v1.1 Float — 悬浮窗、四态状态机、due_date、窗口位置记忆
- 2026-08-10：v1.2 Polish — 标签、Snooze、daily/weekly、events 审计、开机自启、托盘 tooltip badge
- 2026-08-10：v1.1 补丁 — 悬浮窗三态（圆球/面板/挂边）、边缘吸附、逾期高亮、migration 006
- 2026-08-11：悬浮窗交互修复 — 拖拽/点击分离、geometry lock、挂边竖线悬停展开、设置项生效
- 2026-08-11：悬浮窗 UI 审查修复 — peek 态机/光标对齐、拖拽结束吸附、ball 64、dock 8px、modal 互斥
- 2026-08-11：悬浮窗复审修复 — 拖拽吸附路径、clientY DPI、strip unpeek 竞态、settings unpeek
- 2026-08-11：悬浮窗第三轮 — onMoved settle 挂边、float modal 禁用 Teleport、peek leave 跟踪
- 2026-08-11：悬浮窗第四轮 — settle 须主按键抬起；新建/详情互斥；compact modal 限高
- 2026-08-11：全库审查 Wave A — 提醒 advance 跳到未来；启动单次 catch-up；关通知仍推进
- 2026-08-11：全库审查 Wave B — nativeDrag settle、geometry lock 世代、peek 失配 unpeek、拖离 undock 保位
- 2026-08-11：全库审查 Wave C — 取消删除、软删禁提醒、逾期过滤对齐、请求世代、提醒配额
- 2026-08-11：全库审查 Wave D — 启用 CSP；移除 opener；迁移事务化
- 2026-08-11：悬浮窗挂边消失修复 — OS 左键结束拖拽；挂边 HWND 48px 在屏内；圆球强制 64×64 关阴影；顶栏拖拽阈值
- 2026-08-11：浮窗主形态 — default_mode 为 ball/panel 主形态；挂边 48×64；×/− 按主形态回到圆球或托盘
- 2026-08-11：前端 Prettier — `.prettierrc.json` + `npm run format` / `mise run format`
- 2026-08-11：Vue 跳转 — tsconfig 接入 `@vue/typescript-plugin`，使用工作区 TypeScript
- 2026-08-11：类型导入统一为文件顶部 `import type`，去掉内联 `import()`
- 2026-08-11：Rust 跳转 — 根目录 virtual workspace 指向 `src-tauri`，Cargo.lock 上移
- 2026-08-11：撤回根 virtual workspace，恢复 `src-tauri` 独立 crate；Vue TS 插件限制为 vue
- 2026-08-11：助理双窗保留圆球 — FloatHost + chrome/body HWND；默认形态当家；快加；migration 007/008
- 2026-08-12：助理体验优化 — chrome 近缘/换边；temp_body_bounds；设置只切 home；细条+计数；Esc/预览单击；文案与文档
- 2026-08-12：助理手感 — 透明点击穿透、16px 让位、胶囊条、show 幂等、禁非手势抢焦点、拖面板不 undock、列表勾选/逾期色
- 2026-08-12：主窗今日工作台 — 三栏视图+Inspector autosave；归档/回收站；与助理硬分工
- 2026-08-12：v1.3 Workbench 落地 — ViewSidebar/TaskListPane/TaskInspector；创建 due/tags；默认排序设置；快捷键与空态
- 2026-08-12：v1.3 审查修复 — get 软删、pageSize/autosave/due 清空、排序与恢复导航；退役 FilterSidebar/抽屉详情
- 2026-08-13：工程规范 — CI（format/lint/vue-tsc/clippy）、ESLint、移除全局 allow lint；ts-rs 类型导出
- 2026-08-13：可扩展性 — list_workbench_todos、get_allowed_transitions、tags/todo_tags 规范化、list_events 活动流；FloatHost 拆为 session/persistence/window_ops
- 2026-08-13：自查修复 — 助理 addReminder 参数顺序；标签 backfill 失败阻启；设置保存失败不关弹窗；Scheduler 通知失败退避；单实例锁；清理 TodoList/filterPrefs 死代码；技术设计 §1.6 对齐 list_workbench_todos
- 2026-08-13：可用性 — 提醒展示 nextTriggerAt/已关闭；restore 重开提醒；助理保存失败不关详情；todos-changed 跨窗刷新列表与托盘计数
- 2026-08-13：全面审查修复 — todos-changed 仅 dirty 回写；navigateToTodo 切视图定位；今日空态预填 due / CTA 文案按视图
- 2026-08-13：Cargo `default-run = only-todo` — 修复与 export-ts 双 binary 导致 `mise run dev` / tauri cargo run 无法选入口
- 2026-08-13：设置保存 — 自启仅在变更时同步 OS；先落库；OS 失败（如 Windows os error 2）不阻断其它设置
- 2026-08-13：UI 全面优化 — tokens 双主题；settings `ui.theme`/`ui.locale`；vue-i18n；autosave 三态；活动条可折叠；助理关详情 flush；a11y focus-visible / aria-modal
- 2026-08-13：UI 审查修复 — 助理 todos-changed/收起丢改；saveStatus 粘滞与并发 save；flush 失败不关详情；状态动词与托盘随 locale；启动无闪烁；日期跟 i18n
- 2026-08-13：术语更名 — 用户可见「伴侣」→「助理」（en: Companion → Assistant）；代码标识与 `companion.*` settings 不变
- 2026-08-13：主窗顶栏移除「打开助理」；助理仍由托盘 / 启动设置唤起
- 2026-08-13：侧栏主归类顺序 今日→进行中→逾期→已完成→全部；归档/回收站贴底；settings `list.default_view` 冷启动默认归类（出厂 today）
- 2026-08-13：审查修复 — `list.default_view` 非法/tag 回落 Today；侧栏底栏与滚动区分开贴底；Tag 写入回落 today；抽出 `workbenchViews` 常量
- 2026-08-13：桌面化交互 — AppConfirm / AppSelect / AppDateTimePicker / AppContextMenu；禁用系统右键与文件拖入；去掉 datetime-local 与原生 select
- 2026-08-13：AppModal 默认限高并可滚 body，修复设置等高弹窗无法滚动
- 2026-08-13：桌面控件复审 — Select/DateTime Esc 不关父 Modal；外层滚动关浮层；时分改为两位文本输入
- 2026-08-13：助理只读预览 — 去掉就地编辑；单击延迟展示标题/描述；双击进主窗不触发单击
- 2026-08-13：助理 UI 重设计 — 连续面板壳、快加胶囊、行内展开预览、圆球/贴边条微动效
- 2026-08-13：助理进主窗 — 列表单击即时；详情双击/按钮与顶栏入口打开主窗（列表不再双击跳转）
- 2026-08-13：详情预览去掉「在主窗口打开」按钮，仅保留双击与顶栏入口
- 2026-08-13：助理面板去掉 −；× 改为 companion_collapse_to_strip 一律收成条
- 2026-08-13：托盘左键显隐主窗；菜单去掉打开主窗口；显示/隐藏助理互斥一项
- 2026-08-13：审查优化 — 托盘隐藏一律收入托盘；预览关闭过渡；caption/locale sync
- 2026-08-13：助理圆球/贴边条 UI 抛光 — 空闲·活跃·逾期分层、按压悬停、逾期轻呼吸、条内侧 nudge、球↔条短时 Transition
- 2026-08-13：架构蓝图 — 提炼可复用 Tauri+Vue 分层标准（`doc/架构蓝图.md`）；跨项目 Skill `tauri-vue-desktop-arch`
- 2026-08-13：助理 chrome 白框修复 — `data-surface` 仅主窗铺底；球/条去掉外扩 shadow 与全窗 focus 矩形描边
- 2026-08-13：圆球 UI A+B — 仪表盘表面环 + 右上角标计数；空闲/有数/逾期三态；无外扩阴影
- 2026-08-13：贴边条对齐 A+B — 表面手柄 + 内侧 accent 脊 + 球同款实色角标；逾期脊呼吸
- 2026-08-13：面板 × 按来源收起 — 圆球家临时板回球；贴边收条；面板家 × 收条、Esc 不收条
- 2026-08-13：审查优化 — 文档 AC/流程图；Ball 回球去重；主窗默认铺底防 FOUC；× 动态提示；去死 CSS
- 2026-08-14：数据层整理 — 拆 `database/migrations.rs`；`with_tx`；backfill 移出 `Database::new`；软删/恢复与设置批量写入同一事务；蓝图 §7 与技术设计 §4/§8 对齐
- 2026-08-14：数据层审查修复 — 删除死包装 API；闭包禁止再入 Mutex；backfill 先收快照再写；rollback 失败打日志；settings get 一次持锁；set_dock_edge 同事务
- 2026-08-14：规范对齐 — Command 不直连 settings repo；snooze 次数上限上移 Service；审计写入失败打 warn；CI gen-types 漂移检查
- 2026-08-14：开发端口 1420 → 1432（`vite.config.ts` + `tauri.conf.json` devUrl）
- 2026-08-14：模态关闭 — 新建/设置点遮罩不关；dirty 时 Esc/×/取消先确认；确认框保持点遮罩取消；助理预览快加有草稿时失焦/离开不收起
- 2026-08-14：模态审查修复 — 创建中禁止放弃；Esc 栈空卸监听；预览保活改为 `hold_preview`，指针如实上报
- 2026-08-14：最近活动 — 默认收起一行摘要；展开最新在上；payload 带 title；动作·标题·相对时间
- 2026-08-14：主窗改为今日焦点台 — 逾期/进行中/今日到期分组；库模式收纳全部/标签/归档；Inspector 按需滑入；行内快加与跨库搜索
- 2026-08-18：三窗主题一条通道 — 宿主 resolve + 全窗 `set_theme`；CSS 只跟 `prefers-color-scheme`；去掉 `data-theme` 与前端解析
- 2026-08-18：标签 chip 视觉区分 — 列表/Inspector 统一 `#` pill + 边框风格
- 2026-08-18：规划感重建 — `timeMeta.ts` 统一时间语义；`list_time_planner`（接下来/逾期/最久未动）；`TimePlannerPane`；列表时间 token + Inspector 时间块
- 2026-08-18：规划感去噪重构（v3 推翻）— 删除独立 `TimePlannerPane`；Today 恢复逾期/进行中/今日到期三段分组并强化视觉对比；列表时间 token 仅 dueDate 存在时展示（去除“未更新多久”噪声）；Inspector 时间块去重保留；清理相关 i18n 与页面布局
- 2026-08-21：gitignore `src-tauri/bindings/` — ts-rs 编译期残留；类型契约仍为 `src/api/generated/`
- 2026-08-24：CI 扩展 — 前端 `npm run build`；Rust `cargo test`；`workflow_dispatch` + 并发取消；`mise run ci` / `test` / `gen-types-check` / `build-fe`
- 2026-08-24：CD — `release.yml` Windows 构建；`v*` tag / `workflow_dispatch`；Draft GitHub Release（未签名、无 updater）
- 2026-08-24：CD 审查修复 — 钉 `tauri-action@v0.6.2`；Release 并发锁 + 90min 超时；CD 仅打 NSIS（避免 WiX/MSI 下载失败）
- 2026-08-24：CI 修复 — Prettier 全量格式化 + `generated` 加入 ignore；clippy 类型别名/`clamp`；gen-types job 补 Linux 系统依赖
- 2026-08-24：应用内自动更新 — Tauri updater + GitHub `latest.json`；确认后 passive 安装并 `relaunch`；`useAppUpdater` + 设置页检查更新；`release.yml` 签名与自动 Publish
- 2026-08-24：Updater 审查修复 — 插件顺序、流程互斥/checking 状态、indeterminate 进度与 a11y、relaunch 容错、CD Secret 校验、清理误生成文件
- 2026-08-24：发版版本对齐 — 三处版本升至 `1.0.0` 与 `v1.0.0` tag 一致；`release.yml` 增加 tag 与 `tauri.conf.json` 校验
- 2026-08-24：CD 修复 — `release.yml` 签名校验改为 Windows `pwsh` + env 注入，避免 bash 语法/多行私钥导致步骤失败
- 2026-08-28：CD 加固 — `release.yml` 在 `tauri-action` 前增加 updater 签名 dry-run；README/技术设计补充私钥全文与 pubkey 成对说明
- 2026-08-28：发版 — 版本升至 `1.0.4`（含签名 dry-run），打 `v1.0.4` tag
- 2026-08-28：签名修复 — 重新生成 updater 密钥对并更新 `pubkey`；版本 `1.0.5` / tag `v1.0.5`；无密码私钥写入 Actions Secret
- 2026-08-28：启动竞态 — 开库后尽早 `manage(AppState)`；main `visible:false` 延后显示；前端挂载前轮询 `get_settings` 等待后端就绪
- 2026-09-04：发版脚本 — `scripts/bump-version.mjs` 同步四处版本；`npm run bump` / `mise run bump-version`；支持 `--check` / `--tag`
- 2026-09-04：时间选择器 — 打开后实测面板高度再翻转；视口夹紧；浮层 z-index 高于 Modal，避免裁切/挡在遮罩后
- 2026-09-04：国内更新加速 — 发版生成 `latest-cn.json`（安装包 URL 加 gh-proxy 前缀）；updater 双 endpoint（代理优先、GitHub 兜底）
- 2026-09-04：应用图标 — 替换默认 Tauri logo 为 Only Todo 品牌图（teal 勾选）；源图 `app-icon-source.png`，`tauri icon` 生成整套 icons
- 2026-09-04：性能与债清理 — `get_workbench_meta` 合并标签/活动 IPC；列表省略 description；标签 backfill 门闩；移除 `list_time_planner`；TodoService 单测
- 2026-09-04：剥离今天/库双模式 — 删 `list_focus_board` 与焦点分组；默认落点 all；migration 012；搜索时仍藏侧栏
- 2026-09-04：主窗新建 — 顶栏「新建」+ 弹窗（标题优先）；去掉列表行内 Enter；标签页预填 tag；创建后进 Inspector
- 2026-09-04：主窗优先 — 唤出主窗时 `collapse_panel_for_main` 退化钉住面板；主窗已在可与面板同屏；抑制悬停 Preview 回弹；单实例走同一路径
- 2026-09-04：主窗优先审查修复 — 退化失败不阻断出主窗（warn）；仅贴边路径 arm Preview 抑制；文档对齐须 leave→enter
